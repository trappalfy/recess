node_modules/
.next/
out/
artifacts/
test-results/
playwright-report/

# PNG masters for the image pipeline; only the exported WebP is committed.
assets-in/
.env*.local
*.tsbuildinfo
next-env.d.ts

# A separate project that lives in this folder; it is not part of Recess.
NUVO/
